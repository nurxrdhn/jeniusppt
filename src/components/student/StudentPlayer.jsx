import { useMemo, useState } from 'react';
import { SLIDE_SIZES, ratioStyle } from '../../utils/slideSizes';
const demo={title:'Inflasi dan Deflasi',slideSize:SLIDE_SIZES.wide,slides:[{title:'Inflasi',body:'Pengertian, penyebab, dan dampaknya.',background:{type:'css',value:'linear-gradient(135deg,#0b1f46,#1d4ed8)'}},{title:'Penyebab',body:'Permintaan tinggi dan biaya produksi meningkat.',background:{type:'css',value:'linear-gradient(135deg,#ffffff,#dbeafe)'}}],questions:[{type:'pg',question:'Apa itu inflasi?',options:['Harga naik umum','Harga turun','Produksi naik','Ekspor naik'],answer:0},{type:'truefalse',question:'Inflasi dapat menurunkan daya beli.',answer:true}]};
export default function StudentPlayer(){const code=window.location.pathname.split('/play/')[1]||'DEMO';const data=useMemo(()=>{const saved=localStorage.getItem(`jeniusppt_package_${code}`);return saved?JSON.parse(saved):demo},[code]);const[student,setStudent]=useState(null);const[mode,setMode]=useState('slide');const[slide,setSlide]=useState(0);const[question,setQuestion]=useState(0);const[answer,setAnswer]=useState(null);const[correct,setCorrect]=useState(0);const score=data.questions.length?Math.round((correct/data.questions.length)*100):0;if(!student)return <main className="student-page"><form className="student-card" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);setStudent({name:f.get('name'),gender:f.get('gender'),className:f.get('className')})}}><span className="eyebrow">Masuk Kelas</span><h1>{data.title}</h1><input name="name" placeholder="Nama" required/><select name="gender" required><option value="">Gender</option><option>Laki-laki</option><option>Perempuan</option></select><input name="className" placeholder="Kelas" required/><button className="primary-button full">Lanjut →</button></form></main>;if(mode==='result')return <main className="student-page"><section className="student-card center"><span className="eyebrow">Skor</span><div className="big-score">{score}</div><p>{correct} Benar • {data.questions.length-correct} Salah</p></section></main>;if(mode==='quiz'){const q=data.questions[question];const isTF=q.type==='truefalse';const options=isTF?['Benar','Salah']:q.options;return <main className="student-page"><section className="student-slide"><div className="student-meta"><b>{student.name}</b><span>Soal {question+1}/{data.questions.length}</span></div><h1>{q.question}</h1><div className="student-options">{options.map((opt,i)=>{const value=isTF?i===0:i;const isRight=q.answer===value;const picked=answer===value;const show=answer!==null;return <button key={opt} disabled={show} className={show&&isRight?'correct':show&&picked?'wrong':''} onClick={()=>{setAnswer(value);if(isRight)setCorrect(v=>v+1)}}><b>{isTF?(i===0?'✓':'✕'):['A','B','C','D'][i]}</b>{opt}</button>})}</div>{answer!==null&&<div className="answer-result">{answer===q.answer?'✅ Benar':'❌ Salah'}{question<data.questions.length-1?<button onClick={()=>{setQuestion(i=>i+1);setAnswer(null)}}>Lanjut</button>:<button onClick={()=>setMode('result')}>Skor</button>}</div>}</section></main>}const s=data.slides[slide];const bg=s.background||{type:'css',value:'linear-gradient(135deg,#0b1f46,#1d4ed8)'};return <main className="student-page"><section className="student-slide ppt-like" style={{...ratioStyle(data.slideSize||SLIDE_SIZES.wide),...(bg.type==='image'?{backgroundImage:`url(${bg.value})`}:{background:bg.value})}}><div className="student-meta"><b>{student.name}</b><span>Slide {slide+1}/{data.slides.length}</span></div><h1>{s.title}</h1><p>{s.body}</p><div className="student-controls"><button disabled={slide===0} onClick={()=>setSlide(i=>i-1)}>←</button>{slide<data.slides.length-1?<button onClick={()=>setSlide(i=>i+1)}>→</button>:<button onClick={()=>setMode(data.questions.length?'quiz':'result')}>Kuis</button>}</div></section></main>}
