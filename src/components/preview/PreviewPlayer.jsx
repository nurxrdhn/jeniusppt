import { useMemo, useState } from "react";
import { SLIDE_SIZES, ratioStyle } from "../../utils/slideSizes";
import MediaPlayer from "../ui/MediaPlayer";
export default function PreviewPlayer({material,teacher=true}){const timeline=useMemo(()=>[...(material.slides||[]).map((item,index)=>({kind:"slide",item,index})),...(material.questions||[]).map((item,index)=>({kind:"question",item,index})),{kind:"finish",item:null,index:0}],[material]);const[active,setActive]=useState(0);const current=timeline[active];const slideSize=material.slideSize||SLIDE_SIZES.wide;function next(){setActive((i)=>Math.min(i+1,timeline.length-1))}function prev(){setActive((i)=>Math.max(i-1,0))}return <div className="preview-player"><aside className="preview-timeline">{timeline.map((item,index)=><button key={index} className={index===active?"active":""} onClick={()=>setActive(index)}><span>{item.kind==="slide"?index+1:item.kind==="question"?(item.item.type==="truefalse"?"B/S":"PG"):"✓"}</span><b>{item.kind==="slide"?item.item.title:item.kind==="question"?item.item.question:"Selesai"}</b></button>)}</aside><main className="preview-stage">{current.kind==="slide"&&<section key={`slide-${active}`} className={`preview-canvas transition-${current.item.transition||"fade"}`} style={{...ratioStyle(slideSize),...(current.item.background?.type==="image"?{backgroundImage:`url(${current.item.background.value})`}:{background:current.item.background?.value||"linear-gradient(135deg,#ff7a25,#e94d08)"}),textAlign:current.item.textAlign||"left",animationDuration:`${current.item.duration||700}ms`}}><h1 style={{color:current.item.titleColor||"#ffffff"}}>{current.item.title}</h1><p style={{color:current.item.bodyColor||"#e4ecff"}}>{current.item.body}</p><SlideElements elements={current.item.elements||[]}/></section>}{current.kind==="question"&&<section className="preview-question"><span>{current.item.type==="truefalse"?"Benar / Salah":"Pilihan Ganda"}</span><h1>{current.item.question}</h1><div className={current.item.type==="truefalse"?"tf-grid":"answer-grid"}>{(current.item.type==="truefalse"?["Benar","Salah"]:current.item.options).map((option,index)=>{const value=current.item.type==="truefalse"?index===0:index;const isAnswer=current.item.answer===value;return <button key={option} className={teacher&&isAnswer?"selected":""}><b>{current.item.type==="truefalse"?(index===0?"✓":"✕"):["A","B","C","D"][index]}</b>{option}</button>})}</div></section>}{current.kind==="finish"&&<section className="preview-finish"><h1>Selesai</h1><p>{material.slides.length} Slide • {material.questions.length} Soal</p></section>}<div className="preview-controls"><button onClick={prev} disabled={active===0}>←</button><span>{active+1} / {timeline.length}</span><button onClick={next} disabled={active===timeline.length-1}>→</button></div></main></div>}
function SlideElements({elements}){return <div className="free-elements-layer preview-elements">{elements.map((item)=><div key={item.id} className={`free-element ${item.type}`} style={{left:`${item.x}%`,top:`${item.y}%`,width:`${item.w}%`,height:`${item.h}%`,color:item.color,background:item.type==="shape"?item.background:"transparent"}}>{item.type==="text"&&item.text}{item.type==="image"&&<img src={item.src} alt="Elemen slide"/>}{(item.type==="video"||item.type==="audio")&&<MediaPlayer item={item}/>}</div>)}</div>}
