import { Copy, Plus, Trash2 } from 'lucide-react';
import { SLIDE_SIZES, ratioStyle } from '../../utils/slideSizes';
import BackgroundPicker from './BackgroundPicker';
const defaultBg={type:'css',value:'linear-gradient(135deg,#0b1f46,#1d4ed8)'};
export default function PPTEditor({material,updateMaterial}){const slides=material.slides||[];const activeIndex=material.activeSlide||0;const active=slides[activeIndex]||slides[0];const slideSize=material.slideSize||SLIDE_SIZES.wide;function setSlides(next){updateMaterial(material.id,{slides:next})}function updateSlide(patch){setSlides(slides.map((s,i)=>i===activeIndex?{...s,...patch}:s))}function addSlide(){const next=[...slides,{title:'Slide Baru',body:'Mulai mengetik...',background:defaultBg}];setSlides(next);updateMaterial(material.id,{activeSlide:next.length-1})}function copySlide(){const next=[...slides];next.splice(activeIndex+1,0,{...active,title:`${active.title} Copy`});setSlides(next);updateMaterial(material.id,{activeSlide:activeIndex+1})}function deleteSlide(){if(slides.length<=1)return;const next=slides.filter((_,i)=>i!==activeIndex);setSlides(next);updateMaterial(material.id,{activeSlide:Math.max(0,activeIndex-1)})}function setSizeKey(key){updateMaterial(material.id,{slideSize:SLIDE_SIZES[key]})}function swapOrientation(){updateMaterial(material.id,{slideSize:{...slideSize,width:slideSize.height,height:slideSize.width,label:slideSize.width>slideSize.height?'Portrait':'Landscape'}})}const bg=active?.background||defaultBg;return <div className="ppt-editor"><aside className="slide-list"><div className="panel-head"><h3>Slide</h3><button onClick={addSlide}><Plus size={16}/></button></div>{slides.map((s,i)=><button key={i} className={i===activeIndex?'active':''} onClick={()=>updateMaterial(material.id,{activeSlide:i})}><span>{i+1}</span><b>{s.title||'Untitled'}</b></button>)}</aside><main className="slide-stage"><div className="editor-toolbar"><button onClick={addSlide}><Plus size={16}/>Slide</button><button onClick={copySlide}><Copy size={16}/>Copy</button><button onClick={deleteSlide}><Trash2 size={16}/>Hapus</button><select value={Object.keys(SLIDE_SIZES).find(k=>SLIDE_SIZES[k].label===slideSize.label)||'wide'} onChange={e=>setSizeKey(e.target.value)}>{Object.entries(SLIDE_SIZES).map(([key,s])=><option key={key} value={key}>{s.label}</option>)}</select><button onClick={swapOrientation}>Landscape / Portrait</button></div><section className="slide-canvas-wrap"><div className="slide-canvas" style={{...ratioStyle(slideSize),...(bg.type==='image'?{backgroundImage:`url(${bg.value})`}:{background:bg.value})}}><input value={active?.title||''} onChange={e=>updateSlide({title:e.target.value})} className="slide-title-input" placeholder="Judul"/><textarea value={active?.body||''} onChange={e=>updateSlide({body:e.target.value})} className="slide-body-input" placeholder="Mulai mengetik..."/></div></section></main><aside className="properties-panel"><h3>Slide</h3><label>Ukuran</label><select value={slideSize.label} onChange={e=>{const found=Object.values(SLIDE_SIZES).find(s=>s.label===e.target.value);if(found)updateMaterial(material.id,{slideSize:found})}}>{Object.values(SLIDE_SIZES).map(s=><option key={s.label}>{s.label}</option>)}</select><label>Judul</label><input value={active?.title||''} onChange={e=>updateSlide({title:e.target.value})}/><label>Isi</label><textarea value={active?.body||''} onChange={e=>updateSlide({body:e.target.value})}/><BackgroundPicker onPick={background=>updateSlide({background})}/></aside></div>}
